import { EditPageHeaderDirective } from './edit-page-header.directive';

describe('EditPageHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new EditPageHeaderDirective();
    expect(directive).toBeTruthy();
  });
});

import { KeywordsPlaceholderLabelDirective } from './keywords-placeholder-label.directive';

describe('KeywordsPlaceholderLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new KeywordsPlaceholderLabelDirective();
    expect(directive).toBeTruthy();
  });
});

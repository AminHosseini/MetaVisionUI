import { TableColumnValueDirective } from './table-column-value.directive';

describe('TableColumnValueDirective', () => {
  it('should create an instance', () => {
    const directive = new TableColumnValueDirective();
    expect(directive).toBeTruthy();
  });
});

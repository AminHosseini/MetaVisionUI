import { OperationButtonDirective } from './operation-button.directive';

describe('OperationButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new OperationButtonDirective();
    expect(directive).toBeTruthy();
  });
});

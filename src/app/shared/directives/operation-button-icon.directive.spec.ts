import { OperationButtonIconDirective } from './operation-button-icon.directive';

describe('OperationButtonIconDirective', () => {
  it('should create an instance', () => {
    const directive = new OperationButtonIconDirective();
    expect(directive).toBeTruthy();
  });
});

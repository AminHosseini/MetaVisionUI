import { EditPageHeaderButtonDirective } from './edit-page-header-button.directive';

describe('EditPageHeaderButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new EditPageHeaderButtonDirective();
    expect(directive).toBeTruthy();
  });
});
